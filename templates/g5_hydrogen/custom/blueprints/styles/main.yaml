name: Main Section Color & Style
description: Main content colors for the Hydrogen theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#ffffff"
    font-size:
      type: input.text
      label: Font Size
      default: "15px"
    text-color:
      type: input.colorpicker
      label: Text
      default: "#666666"